{
  "private": "true",
  "name": "@shared/database",
  "author": "Maciej Garncarski",
  "type": "module",
  "version": "1.0.0",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "pnpm prisma generate && pnpm clean && pnpm tsc -b",
    "lint": "eslint --fix .",
    "format": "prettier --write --cache .",
    "lint-format": "pnpm lint && pnpm format",
    "check-types": "pnpm tsc --skipLibCheck --noEmit",
    "generate-prisma": "pnpm prisma generate",
    "db-pull": "pnpm prisma db pull"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.3.0",
    "@prisma/client": "^7.3.0",
    "prisma": "^7.3.0"
  },
  "devDependencies": {
    "@shared/eslint-prettier": "workspace:*",
    "@types/node": "catalog:",
    "copyfiles": "^2.4.1",
    "rimraf": "^6.1.2",
    "typescript": "catalog:"
  }
}
